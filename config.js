// Leave blank when using the on-page ngrok connection setting.
window.APP_CONFIG = {
  API_BASE_URL: "",
  // Keep this value in sync with solver.parameters.max_time_in_seconds in app.py.
  SOLVER_MAX_SECONDS: 60
};
